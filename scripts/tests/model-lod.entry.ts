// Bundled by model-lod.test.mjs so plain node can import the TS module.
export {
  apparentPixelHeight,
  buildLodLevels,
  resolveLevel,
  wantedLevel,
} from '../../src/babylon/modelLod'
export { MODEL_LODS, lodLevelsFor } from '../../src/data/modelLods'
