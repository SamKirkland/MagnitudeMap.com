/**
 * Legacy stylized rocket generator — rockets now come from Sketchfab
 * (`npm run fetch-sketchfab`). Kept so the npm script does not break; it no-ops.
 *
 * Run: npm run generate-rockets
 */
console.log(
  'generate-rockets: no-op — rocket GLBs are downloaded via npm run fetch-sketchfab',
)
