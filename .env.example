# Optional. Sketchfab Settings → Password → API Token
# https://sketchfab.com/settings/password
# Used only if you query the Sketchfab API yourself. Not required to build or run the site.
SKETCHFAB_API_TOKEN=
