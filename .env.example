# Sketchfab Data API — Settings → Password → API Token
# https://sketchfab.com/settings/password
# Agents use this to search and download models. Not required to build or run the site.
# Never commit this file's real value.
SKETCHFAB_API_TOKEN=
